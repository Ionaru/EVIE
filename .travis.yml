os:
- linux
language: node_js
node_js:
- '7'
- '6'
- '5'
- '4.2.6'
env:
  - SUB_DIR=client
  - SUB_DIR=server
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
script:
- cd $SUB_DIR
- yarn install
- yarn run "lint"
cache:
  directories:
  - $HOME/.cache/yarn
