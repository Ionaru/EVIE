{
    "extends": "../tsconfig.json",
    "angularCompilerOptions": {
        // The CLI will also output a warning, disable the Angular check so the compiler doesn't crash.
        "disableTypeScriptVersionCheck": true
    },
    "compilerOptions": {
        "outDir": "../out-tsc/app",
        "types": []
    },
    "files": [
        "main.ts"
    ]
}
